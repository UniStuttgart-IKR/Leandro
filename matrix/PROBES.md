<!-- SPDX-License-Identifier: MIT -->
<!-- GENERATED by scripts/ioctl-matrix.sh probes -- do not edit. -->
# Probes: one row per feature path

```
driver:  610.57.04
gpu:     NVIDIA GeForce RTX 2070
arch:    Turing (compute 7.5)
kernel:  7.1.8-arch1-3
date:    2026-08-20T16:07:08Z
commit:  9fb7da8 (working tree modified)
```

Regenerated by `scripts/ioctl-matrix.sh probes`, by reading the headers of
the files in `probe/matrix/`. There is no registry: the set of probes is the
set of files, and adding a probe is adding one file.

A probe is one feature path, minimal and deterministic. One probe may cover
several libraries (a GL probe covers glcore, glsi, tls and the allocator at
once); one library may need several probes (EGL has a separate external
platform module per windowing system, and they are different code paths).

**Every probe has a criterion beyond its exit code.** The reason is not
rigour for its own sake: a missing library fails with ENOENT before any
ioctl is issued, so the tracer sees nothing at all and an empty trace looks
like a feature nobody used. "Denied", "absent" and "silently degraded" are
three different findings and only a checked result tells them apart.

## compute

| probe | libraries covered | entry API | functional criterion | status |
|---|---|---|---|---|
| `cuda-core` | libcuda | `cuInit, cuCtxCreate, cuMemAlloc/cuMemcpyHtoD/DtoH` | nvprobe reports "stage 2 ok", which it prints only after reading its buffer back off the card and comparing it | ready |
| `cuda-gdb` | libcudadebugger<br>libcuda | `cuda-gdb --batch -ex run (CUDA debugger back end)` | the inferior ran to completion UNDER the debugger and still reported "stage 3 ok" -- the kernel produced the right numbers with the debugger back end attached | ready |
| `cuda-hostreg` | libcuda | `cuMemHostRegister / cuMemHostGetDevicePointer` | hostregprobe's own check that the registered pages carry data both ways | ready |
| `cuda-jit` | libcuda<br>libnvidia-nvvm<br>libnvidia-ptxjitcompiler<br>libnvidia-tileiras<br>libnvidia-nvvm70 | `cuModuleLoadData (PTX), cuLaunchKernel` | nvprobe reports "stage 3 ok" -- the kernel's result is compared against the value it must produce | ready |
| `cuda-launch` | libcuda | `cuLaunchKernel x N, cuCtxSynchronize (CU_CTX_SCHED_BLOCKING_SYNC)` | nvprobe reports "stage 4 ok" and a non-zero iteration count in its own ITERS= line | ready |
| `cuda-managed` | libcuda | `cuMemAllocManaged, UVM_*` | managedprobe's own correctness check on the managed buffer (it compares what the GPU wrote with what the CPU reads) | ready |
| `cuda-torch` | libcuda<br>libnvidia-nvvm<br>libnvidia-ptxjitcompiler | `torch.cuda + a cuDNN convolution` | torchprobe reports "stage 5 ok" after comparing its own tensor result | ready |
| `opencl` | libnvidia-opencl | `clGetPlatformIDs -> clBuildProgram -> clEnqueueNDRangeKernel` | all 4096 elements of a vector add come back correct -- an absent vendor library gives a clean "no platform" and zero ioctls, which only a computed result tells apart | ready |

## nvml

| probe | libraries covered | entry API | functional criterion | status |
|---|---|---|---|---|
| `nvml` | libnvidia-ml<br>libnvidia-cfg | `nvidia-smi -q (NVML)` | the report names this card's product and its driver version -- a degraded NVML still prints a table | ready |

## video

| probe | libraries covered | entry API | functional criterion | status |
|---|---|---|---|---|
| `nvdec` | libnvcuvid<br>libcuda | `-hwaccel cuda (NVDECODE / cuvid)` | the frames decoded on the card equal the frames encoded -- a silent fallback to the software decoder would also produce frames, so the CUDA hardware frames context is required and its absence fails the run | ready |
| `nvenc` | libnvidia-encode<br>libcuda | `h264_nvenc (NVENCODE API)` | an H.264 elementary stream large enough to hold the frames comes back, and ffmpeg reports no encoder error -- a refused NVENC session exits non-zero with an empty file, which is the failure this distinguishes | ready |
| `nvfbc` | libnvidia-fbc | `NvFBCCreateInstance (Capture SDK)` | none -- no workload | declared-unsupported: workload not procurable in this environment |
| `nvofa` | libnvidia-opticalflow | `NvOFAPICreateInstanceCuda (Video Codec SDK)` | none -- no workload | declared-unsupported: workload not procurable in this environment |

## gl

| probe | libraries covered | entry API | functional criterion | status |
|---|---|---|---|---|
| `gl-enum` | libGLX_nvidia<br>libnvidia-glcore | `glXQueryServerString / glXChooseFBConfig (glxinfo -B)` | the OpenGL renderer string names NVIDIA and direct rendering is on -- llvmpipe answers this call too | ready |
| `gl-render` | libGLX_nvidia<br>libnvidia-glcore<br>libnvidia-glsi<br>libnvidia-tls<br>libnvidia-gpucomp<br>libnvidia-allocator | `GLX context + FBO rendering` | a non-zero glmark2 score over a FIXED 100 frames, i.e. frames were drawn -- an enumerating client would produce ioctls without ever rendering anything | ready |
| `gles` | libEGL_nvidia<br>libnvidia-eglcore<br>libnvidia-glsi | `eglBindAPI(EGL_OPENGL_ES_API) + GLES2 enumeration` | the GLES renderer string names NVIDIA | ready |

## egl

| probe | libraries covered | entry API | functional criterion | status |
|---|---|---|---|---|
| `egl-gbm` | libEGL_nvidia<br>libnvidia-eglcore<br>libnvidia-egl-gbm<br>libnvidia-glsi<br>libnvidia-tls<br>libnvidia-glvkspirv | `eglGetPlatformDisplayEXT(gbm) + pbuffer + GLES2` | a pixel cleared to 3377bb comes back as 3377bb from a 64x64 pbuffer -- eglInitialize alone would pass while the platform module underneath fell back silently | ready |
| `egl-wayland` | libEGL_nvidia<br>libnvidia-eglcore<br>libnvidia-egl-wayland<br>libnvidia-glsi<br>libnvidia-tls<br>libnvidia-glvkspirv | `eglGetPlatformDisplayEXT(wayland) + pbuffer + GLES2` | a pixel cleared to 3377bb comes back as 3377bb from a 64x64 pbuffer -- eglInitialize alone would pass while the platform module underneath fell back silently | ready |
| `egl-xcb` | libEGL_nvidia<br>libnvidia-eglcore<br>libnvidia-egl-xcb<br>libnvidia-glsi<br>libnvidia-tls<br>libnvidia-glvkspirv | `eglGetPlatformDisplayEXT(xcb) + pbuffer + GLES2` | a pixel cleared to 3377bb comes back as 3377bb from a 64x64 pbuffer -- eglInitialize alone would pass while the platform module underneath fell back silently | ready |
| `egl-xlib` | libEGL_nvidia<br>libnvidia-eglcore<br>libnvidia-egl-xlib<br>libnvidia-glsi<br>libnvidia-tls<br>libnvidia-glvkspirv | `eglGetPlatformDisplayEXT(xlib) + pbuffer + GLES2` | a pixel cleared to 3377bb comes back as 3377bb from a 64x64 pbuffer -- eglInitialize alone would pass while the platform module underneath fell back silently | ready |

## vulkan

| probe | libraries covered | entry API | functional criterion | status |
|---|---|---|---|---|
| `vk-enum` | libGLX_nvidia<br>libnvidia-glvkspirv | `vkEnumeratePhysicalDevices (vulkaninfo --summary)` | a device with NVIDIA's driver name is listed -- an unresolved ICD leaves only llvmpipe, which also answers | ready |
| `vk-offscreen` | libGLX_nvidia<br>libnvidia-glvkspirv<br>libnvidia-gpucomp<br>libnvidia-allocator | `Vulkan compute + hwupload/hwdownload` | raw frames come back out of the filter chain at the expected byte count -- an initialisation-only run would produce ioctls and no pixels | ready |
| `vk-rt` | libnvidia-rtcore<br>libGLX_nvidia<br>libnvidia-glvkspirv | `vkCreateDevice with VK_KHR_acceleration_structure` | the device is created AND hands back a non-zero device address for a buffer -- device creation alone does not prove the address space behind it | ready |
| `vk-sc` | libnvidia-vksc-core | `vkscCreateInstance (Vulkan SC loader)` | none -- no workload | declared-unsupported: no workload exists |

## compat

| probe | libraries covered | entry API | functional criterion | status |
|---|---|---|---|---|
| `gl-32bit` | lib32:libGLX_nvidia<br>lib32:libEGL_nvidia<br>lib32:libnvidia-glcore<br>lib32:libnvidia-eglcore<br>lib32:libnvidia-glsi<br>lib32:libnvidia-tls<br>lib32:libnvidia-gpucomp<br>lib32:libnvidia-allocator<br>lib32:libnvidia-glvkspirv<br>lib32:libcuda<br>lib32:libnvidia-nvvm<br>lib32:libnvidia-ptxjitcompiler<br>lib32:libnvidia-tileiras | `any 32-bit client (glxgears)` | none -- not traceable here | blocked: needs kernel-side trace point |
| `pkcs11` | libnvidia-pkcs11<br>libnvidia-pkcs11-openssl3 | `C_GetFunctionList (PKCS#11)` | none -- no workload | declared-unsupported: no workload exists |

## kms

| probe | libraries covered | entry API | functional criterion | status |
|---|---|---|---|---|
| `kms-nvkms` | nvidia-modeset.ko<br>nvidia-drm.ko | `NVKMS ioctls (in-kernel RM client)` | none -- not observable from userspace | blocked: needs kernel-side trace point |

## The 32-bit set and the NVKMS axis

Both rows above are `blocked: needs kernel-side trace point`, and the reason
is the same in two different shapes.

The tracer is a 64-bit LD_PRELOAD interposer and cannot be preloaded into a
32-bit client. A 32-bit tracer is explicitly not the answer: it would be a
second instrument measuring the same boundary, with its own offsets to keep
correct. The instrument that covers both bit widths at once sits on the
kernel side, and this tree has none on the host. If one appears, ONE 32-bit
probe -- glxgears or equivalent -- validates the trace path for the whole
20-library set.

nvidia_modeset and nvidia_drm are not a userspace column at all -- as KERNEL
MODULES. NVKMS is an in-kernel RM client, so its RM traffic never crosses a
userspace ioctl boundary and no interposer can see it whatever the client
does. That is what this row is blocked on, and it is NOT the same statement
as "NVKMS is invisible": `/dev/nvidia-modeset` is a userspace node the GL
and Vulkan libraries open themselves, it is traced and gated since
2026-08-20, and the catalogue has a section for it. What those calls still
lack is names -- NVKMS command numbers are a namespace of their own and do
not resolve against `ctrl*.h`. The DRM surface a client presents to
`nvidia_drm` IS measurable
natively and this tree already measures it -- `probe/run/drmtrace.sh`, which
decodes the numbers out of the headers that apply because strace names
NVIDIA's private DRM numbers after other vendors' drivers. That is a
different boundary and stays a separate report.

## Libraries with no probe

Every library in the staged set, and the probe that covers it. A library
with no probe is a row in the catalogue with no measurement behind it,
which is a finding rather than a gap in this table.

| library | staged | covered by |
|---|---|---|
| `libEGL_nvidia` | 64-bit |  egl-gbm egl-wayland egl-xcb egl-xlib gl-32bit gles |
| `libGLESv1_CM_nvidia` | 64-bit |  -- no probe |
| `libGLESv2_nvidia` | 64-bit |  -- no probe |
| `libGLX_nvidia` | 64-bit |  gl-32bit gl-enum gl-render vk-enum vk-offscreen vk-rt |
| `libcuda` | 64-bit |  cuda-core cuda-gdb cuda-hostreg cuda-jit cuda-launch cuda-managed cuda-torch gl-32bit nvdec nvenc |
| `libcudadebugger` | 64-bit |  cuda-gdb |
| `libglxserver_nvidia` | **not staged** |  -- no probe |
| `libnvcuvid` | 64-bit |  nvdec |
| `libnvidia-allocator` | 64-bit |  gl-32bit gl-render vk-offscreen |
| `libnvidia-api` | **not staged** |  -- no probe |
| `libnvidia-cfg` | 64-bit |  nvml |
| `libnvidia-egl-gbm` | 64-bit |  egl-gbm |
| `libnvidia-egl-wayland` | 64-bit |  egl-wayland |
| `libnvidia-egl-wayland2` | 64-bit |  -- no probe |
| `libnvidia-egl-xcb` | 64-bit |  egl-xcb |
| `libnvidia-egl-xlib` | 64-bit |  egl-xlib |
| `libnvidia-eglcore` | 64-bit |  egl-gbm egl-wayland egl-xcb egl-xlib gl-32bit gles |
| `libnvidia-encode` | 64-bit |  nvenc |
| `libnvidia-fbc` | 64-bit |  nvfbc |
| `libnvidia-glcore` | 64-bit |  gl-32bit gl-enum gl-render |
| `libnvidia-glsi` | 64-bit |  egl-gbm egl-wayland egl-xcb egl-xlib gl-32bit gl-render gles |
| `libnvidia-glvkspirv` | 64-bit |  egl-gbm egl-wayland egl-xcb egl-xlib gl-32bit vk-enum vk-offscreen vk-rt |
| `libnvidia-gpucomp` | 64-bit |  gl-32bit gl-render vk-offscreen |
| `libnvidia-ml` | 64-bit |  nvml |
| `libnvidia-ngx` | **not staged** |  -- no probe |
| `libnvidia-nvvm` | 64-bit |  cuda-jit cuda-torch gl-32bit |
| `libnvidia-nvvm70` | 64-bit |  cuda-jit |
| `libnvidia-opencl` | 64-bit |  opencl |
| `libnvidia-opticalflow` | 64-bit |  nvofa |
| `libnvidia-pkcs11` | 64-bit |  pkcs11 |
| `libnvidia-pkcs11-openssl3` | 64-bit |  pkcs11 |
| `libnvidia-present` | **not staged** |  -- no probe |
| `libnvidia-ptxjitcompiler` | 64-bit |  cuda-jit cuda-torch gl-32bit |
| `libnvidia-rtcore` | 64-bit |  vk-rt |
| `libnvidia-sandboxutils` | **not staged** |  -- no probe |
| `libnvidia-tileiras` | 64-bit |  cuda-jit gl-32bit |
| `libnvidia-tls` | 64-bit |  egl-gbm egl-wayland egl-xcb egl-xlib gl-32bit gl-render |
| `libnvidia-vksc-core` | 64-bit |  vk-sc |
| `libnvoptix` | **not staged** |  -- no probe |
| `libvdpau_nvidia` | **not staged** |  -- no probe |
