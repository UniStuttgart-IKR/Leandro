# SPDX-License-Identifier: MIT
# policy.txt, from the run -- n4-profile policy, 2026-08-21.
vm0: vhost-user-nvrm: VRAM profile 1536 MiB for this VM = 1280 MiB guest FB + 256 MiB reserved for RM's own device memory (LEA_VRAM_PROFILE_MIB / LEA_VRAM_RESERVE_MIB). The guest is told 1280 MiB and is refused at the same number; the reservation is not allocated, it is FB the guest is never offered. Nothing here checks the card or a sibling VM, so profiles that sum past the card are accepted.
vm1: vhost-user-nvrm: VRAM profile 1536 MiB for this VM = 1280 MiB guest FB + 256 MiB reserved for RM's own device memory (LEA_VRAM_PROFILE_MIB / LEA_VRAM_RESERVE_MIB). The guest is told 1280 MiB and is refused at the same number; the reservation is not allocated, it is FB the guest is never offered. Nothing here checks the card or a sibling VM, so profiles that sum past the card are accepted.
vm2: vhost-user-nvrm: VRAM profile 1536 MiB for this VM = 1280 MiB guest FB + 256 MiB reserved for RM's own device memory (LEA_VRAM_PROFILE_MIB / LEA_VRAM_RESERVE_MIB). The guest is told 1280 MiB and is refused at the same number; the reservation is not allocated, it is FB the guest is never offered. Nothing here checks the card or a sibling VM, so profiles that sum past the card are accepted.
vm3: vhost-user-nvrm: VRAM profile 1536 MiB for this VM = 1280 MiB guest FB + 256 MiB reserved for RM's own device memory (LEA_VRAM_PROFILE_MIB / LEA_VRAM_RESERVE_MIB). The guest is told 1280 MiB and is refused at the same number; the reservation is not allocated, it is FB the guest is never offered. Nothing here checks the card or a sibling VM, so profiles that sum past the card are accepted.
