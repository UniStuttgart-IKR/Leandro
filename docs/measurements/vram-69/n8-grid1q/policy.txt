# SPDX-License-Identifier: MIT
# policy.txt, from the run -- n8-grid1q policy, 2026-08-21.
vm0: vhost-user-nvrm: vGPU-shaped type RTX2070-1Q for this VM (LEA_VGPU_TYPE): profile 1024 MiB = 512 MiB guest FB + 512 MiB reserved. Both numbers come from the CARD's catalogue, not from an operator: the guest FB is a whole number of VMMU segments and every VM on this card has the same size. The guest's card is called "Leandro RTX2070-1Q".
vm1: vhost-user-nvrm: vGPU-shaped type RTX2070-1Q for this VM (LEA_VGPU_TYPE): profile 1024 MiB = 512 MiB guest FB + 512 MiB reserved. Both numbers come from the CARD's catalogue, not from an operator: the guest FB is a whole number of VMMU segments and every VM on this card has the same size. The guest's card is called "Leandro RTX2070-1Q".
vm2: vhost-user-nvrm: vGPU-shaped type RTX2070-1Q for this VM (LEA_VGPU_TYPE): profile 1024 MiB = 512 MiB guest FB + 512 MiB reserved. Both numbers come from the CARD's catalogue, not from an operator: the guest FB is a whole number of VMMU segments and every VM on this card has the same size. The guest's card is called "Leandro RTX2070-1Q".
vm3: vhost-user-nvrm: vGPU-shaped type RTX2070-1Q for this VM (LEA_VGPU_TYPE): profile 1024 MiB = 512 MiB guest FB + 512 MiB reserved. Both numbers come from the CARD's catalogue, not from an operator: the guest FB is a whole number of VMMU segments and every VM on this card has the same size. The guest's card is called "Leandro RTX2070-1Q".
vm4: vhost-user-nvrm: vGPU-shaped type RTX2070-1Q for this VM (LEA_VGPU_TYPE): profile 1024 MiB = 512 MiB guest FB + 512 MiB reserved. Both numbers come from the CARD's catalogue, not from an operator: the guest FB is a whole number of VMMU segments and every VM on this card has the same size. The guest's card is called "Leandro RTX2070-1Q".
vm5: vhost-user-nvrm: vGPU-shaped type RTX2070-1Q for this VM (LEA_VGPU_TYPE): profile 1024 MiB = 512 MiB guest FB + 512 MiB reserved. Both numbers come from the CARD's catalogue, not from an operator: the guest FB is a whole number of VMMU segments and every VM on this card has the same size. The guest's card is called "Leandro RTX2070-1Q".
vm6: vhost-user-nvrm: vGPU-shaped type RTX2070-1Q for this VM (LEA_VGPU_TYPE): profile 1024 MiB = 512 MiB guest FB + 512 MiB reserved. Both numbers come from the CARD's catalogue, not from an operator: the guest FB is a whole number of VMMU segments and every VM on this card has the same size. The guest's card is called "Leandro RTX2070-1Q".
vm7: vhost-user-nvrm: vGPU-shaped type RTX2070-1Q for this VM (LEA_VGPU_TYPE): profile 1024 MiB = 512 MiB guest FB + 512 MiB reserved. Both numbers come from the CARD's catalogue, not from an operator: the guest FB is a whole number of VMMU segments and every VM on this card has the same size. The guest's card is called "Leandro RTX2070-1Q".
